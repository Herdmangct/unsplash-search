import React from "react";

const styles = {
  credit: {
    textAlign: "center",
    color: "#231f21",
    textTransform: "uppercase"
  }
};

const Credit = props => {
  return <h3 style={styles.credit}>Credit Unsplash</h3>;
};

export default Credit;
